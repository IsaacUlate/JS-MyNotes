
const average = 7;

const student_loan = (average >= 8) ? 'Approved' : 'Rejected';

console.log(`Resultado: ${student_loan}`);

let max = 0;

const a = 5;
const b = 10;
const c = 15;

max = a > b ? a : b;
max = max > c ? max : c;

console.log(`Max num: ${max}`);